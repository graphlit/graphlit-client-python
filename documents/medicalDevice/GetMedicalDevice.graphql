query GetMedicalDevice($id: ID!, $correlationId: String) {
  medicalDevice(id: $id, correlationId: $correlationId) {
    id
    name
    alternateNames
    creationDate
    uri
    description
    identifier
    thing
    relevance
  }
}
