query QueryMedicalDevices($filter: MedicalDeviceFilter) {
  medicalDevices(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      thing
      relevance
    }
  }
}
