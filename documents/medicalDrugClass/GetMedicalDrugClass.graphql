query GetMedicalDrugClass($id: ID!) {
  medicalDrugClass(id: $id) {
    id
    name
    alternateNames
    creationDate
    uri
    description
    identifier
    thing
    relevance
  }
}
