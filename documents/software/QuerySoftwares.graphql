query QuerySoftwares($filter: SoftwareFilter!) {
  softwares(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      releaseDate
      developer
    }
  }
}