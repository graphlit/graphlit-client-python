query GetLabel($id: ID!, $correlationId: String) {
  label(id: $id, correlationId: $correlationId) {
    id
    name
    user {
      id
    }
    description
    creationDate
    feeds {
      id
      name
    }
  }
}
