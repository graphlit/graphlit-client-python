query GetEmotion($id: ID!, $correlationId: String) {
  emotion(id: $id, correlationId: $correlationId) {
    id
    name
    user {
      id
    }
    description
    creationDate
    feeds {
      id
      name
    }
  }
}
