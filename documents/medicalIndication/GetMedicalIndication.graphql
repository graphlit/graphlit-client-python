query GetMedicalIndication($id: ID!) {
  medicalIndication(id: $id) {
    id
    name
    alternateNames
    creationDate
    uri
    description
    identifier
    thing
    relevance
  }
}
