query QueryMedicalIndications($filter: MedicalIndicationFilter) {
  medicalIndications(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      uri
      description
      identifier
      thing
      relevance
    }
  }
}
