query QueryPersons($filter: PersonFilter!) {
  persons(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      email
      givenName
      familyName
    }
  }
}