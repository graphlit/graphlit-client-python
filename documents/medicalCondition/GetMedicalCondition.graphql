query GetMedicalCondition($id: ID!) {
  medicalCondition(id: $id) {
    id
    name
    alternateNames
    creationDate
    uri
    description
    identifier
    thing
    relevance
  }
}
