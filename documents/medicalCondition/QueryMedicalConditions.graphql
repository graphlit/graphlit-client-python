query QueryMedicalConditions($filter: MedicalConditionFilter) {
  medicalConditions(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      uri
      description
      identifier
      thing
      relevance
    }
  }
}
