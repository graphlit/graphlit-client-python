mutation UpsertSkill($skill: SkillInput!) {
  upsertSkill(skill: $skill) {
    id
    name
    state
    identifier
    skillOwner
    arguments {
      name
      description
      required
    }
  }
}
