query GetInvestmentFund($id: ID!, $correlationId: String) {
  investmentFund(id: $id, correlationId: $correlationId) {
    id
    name
    creationDate
    owner {
      id
    }
    state
    alternateNames
    uri
    description
    identifier
    thing
    feeds {
      id
      name
    }
  }
}
