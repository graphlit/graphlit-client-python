query GetMedicalContraindication($id: ID!) {
  medicalContraindication(id: $id) {
    id
    name
    alternateNames
    creationDate
    uri
    description
    identifier
    thing
    relevance
  }
}
