mutation IngestEvent($markdown: String!, $name: String, $description: String, $eventDate: DateTime, $id: ID, $identifier: String, $collections: [EntityReferenceInput!], $correlationId: String) {
  ingestEvent(name: $name, description: $description, eventDate: $eventDate, markdown: $markdown, id: $id, identifier: $identifier, collections: $collections, correlationId: $correlationId) {
    id
    name
    state
    type
    fileType
    mimeType
    uri
    identifier
    collections {
      id
      name
    }
    observations {
      id
      type
      observable {
        id
        name
      }
      related {
        id
        name
      }
      relatedType
      relation
      occurrences {
        type
        confidence
        startTime
        endTime
        pageIndex
        turnIndex
        boundingBox {
          left
          top
          width
          height
        }
      }
      state
    }
  }
}
