query QueryEvents($filter: EventFilter!) {
  events(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      startDate
      endDate
      price
    }
  }
}