query QueryMedicalTests($filter: MedicalTestFilter) {
  medicalTests(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      thing
      relevance
    }
  }
}
