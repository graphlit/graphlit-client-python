query GetMedicalTest($id: ID!, $correlationId: String) {
  medicalTest(id: $id, correlationId: $correlationId) {
    id
    name
    alternateNames
    creationDate
    uri
    description
    identifier
    thing
    relevance
  }
}
