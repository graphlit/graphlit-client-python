query QueryMedicalTherapies($filter: MedicalTherapyFilter) {
  medicalTherapies(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      thing
      relevance
    }
  }
}
