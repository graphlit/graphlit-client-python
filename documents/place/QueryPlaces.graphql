query QueryPlaces($filter: PlaceFilter) {
  places(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      thing
      address {
        streetAddress
        city
        region
        country
        postalCode
      }
    }
  }
}