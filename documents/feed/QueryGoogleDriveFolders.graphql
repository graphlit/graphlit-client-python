query QueryGoogleDriveFolders($properties: GoogleDriveFoldersInput!, $folderId: ID, $driveId: ID) {
  googleDriveFolders(properties: $properties, folderId: $folderId, driveId: $driveId) {
    results {
      folderName
      folderId
      folderPath
    }
  }
}
