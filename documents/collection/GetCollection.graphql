query GetCollection($id: ID!) {
  collection(id: $id) {
    id
    name
    creationDate
    relevance
    owner {
      id
    }
    state
    type
    contents {
      id
      name
    }
  }
}
