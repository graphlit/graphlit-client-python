query QueryMedicalProcedures($filter: MedicalProcedureFilter) {
  medicalProcedures(filter: $filter) {
    results {
      id
      name
      alternateNames
      creationDate
      thing
      relevance
    }
  }
}
